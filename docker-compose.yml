version: "3.9"

services:
  app:
    build: .
    ports:
      - "8081:8081"
    environment:
      - DATABASE_URL=file:./dev.db
      - JWT_SECRET=change-me
      - BOT_TOKEN=${BOT_TOKEN}
      - PORT=4000
    volumes:
      - sqlite_data:/app/backend/prisma

volumes:
  sqlite_data:
